USE <%= version.schema %>;

-- Start version rollback
RENAME TABLE <%= schema_version %> TO <%= schema_version.prev %>_ROLLBACK;

<%= version.rollback_sql.strip %>

-- End version rollback
RENAME TABLE <%= schema_version.prev %>_ROLLBACK TO <%= schema_version.prev %>;